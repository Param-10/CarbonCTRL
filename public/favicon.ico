# This is a placeholder for favicon.ico - browsers will use the SVG version
# To generate a proper .ico file, you would need to convert the SVG using a tool
# For now, the SVG favicon will work in all modern browsers 